server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # This is the magic line for Quartz
        try_files $uri $uri.html $uri/ =404;
    }

    # Custom error page (optional)
    error_page 404 /404.html;
}
